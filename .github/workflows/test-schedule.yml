name: Scheduler Test

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "event=${{ github.event_name }}"
          date
