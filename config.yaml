# 全局設定
settings:
  headless: true
  timeout: 20000          # 頁面載入超時（毫秒）
  wait_after_load: 1500   # 載入後再等一陣（有啲網站要 JS 渲染）

targets:
  - id: hells_nether_deck
    name: "Hells Nether Deck Set"
    url: "https://lastchancetoy.com/collections/beybladex"
    method: "text"
    value: "UX-21"
    # 可選：額外確認有貨字眼（更準）
    # in_stock_keywords: ["加入購物車", "Add to Cart", "現貨"]

  # 可以繼續加多個
  # - id: example_2
  #   name: "另一件商品"
  #   url: "https://..."
  #   method: "css"
  #   value: ".product-title"
